spring:
  datasource:
    username: root
    password: sandara@123
    url: jdbc:mysql://localhost:3306/ticketingDb

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true